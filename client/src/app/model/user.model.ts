export interface User {
  id?: number; // Unique identifier for the user
  username: string;
  password: string;
  email: string;
  firstName: string;
  lastName: string;
  bio: string;
  phoneNumber: string;
}
